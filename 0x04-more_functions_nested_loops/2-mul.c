#include "main.h"
#include <stdio.h>

/**
 * mul  - multiple two intergers
 * @a: The first number to multiply
 * @b: The second number to multiply with first number
 * Return: The result of multiplication a param and b param
 */

int mul(int a, int b)
{

	return (a * b);
}
